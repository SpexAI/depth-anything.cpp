# Python dependencies for CONVERSION and PARITY checks only.
# Inference (da3-cli / libdepthanything.so) needs NONE of this.
#
# You also need the upstream Depth Anything 3 package for conversion/reference:
#   pip install "git+https://github.com/bytedance-seed/depth-anything-3"
# (it pulls in cv2/opencv-python-headless and the model definitions).

torch>=2.4            # CPU build is fine: torch==2.x+cpu
numpy>=1.26
gguf>=0.19.0          # GGUF writer/reader
onnx>=1.16           # scripts/convert_da2_onnx_to_gguf.py
huggingface_hub>=0.24 # scripts/download_model.py
safetensors>=0.4
einops>=0.8
pillow>=10            # image IO in scripts
matplotlib>=3.8       # benchmarks/make_plots.py, make_demo.py
opencv-python-headless>=4.9  # cv2 — bit-exact resize parity in conversion
